# 0x0A. Python - Inheritance

This project covers the basics of class inheritance in Python:
- Understanding how classes can inherit from other classes.
- Using `isinstance`, `issubclass`, and `type`.
- Method overriding and base class initialization.
- Creating abstract-like base classes.

Each task below is fully PEP8 compliant and designed for Holberton checker.
